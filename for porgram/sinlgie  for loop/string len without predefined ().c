#include<stdio.h>
int main()
{
    char str[100];
    int i,len=0;
printf("Enter the String:");
scanf("%s",&str);
for(i=0;i<=str[i];i++)
{
    len++;
}
   printf("The Length of String is : %d",len);
    return 0;
}